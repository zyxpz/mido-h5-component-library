## getUrlData
- 获取url链接所带参数，可传入链接，默认获取现有url

```js
import { getUrlData } from 'mido-h5-cp';

const urlData = getUrlData();

console.log(urlData);
```

- [效果](https://zyxpz.github.io/mido-h5-cp/dist/web/getUrlData/Basic?data=1&m=2&x=3&l=4)